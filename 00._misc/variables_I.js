// use const when possible
const scheculedBreakTime = "09:27";
let consoleLogCounter = 0;

console.log("Let's take a break at: ", scheculedBreakTime);
consoleLogCounter++;

// Variabler: String, Number, Boolean, BigInteger, Undefined, null, Object og Symbol

// Object indeholder: Object, Array og date

// Type coercion

// The 3 ways to make strings

console.log("This is the first");

console.log('This is the second');

console.log(`This is the third`);

consoleLogCounter += 3;

// Insert variable directly into string

console.log(`Example: ${scheculedBreakTime}`)

consoleLogCounter++;

console.log(consoleLogCounter)